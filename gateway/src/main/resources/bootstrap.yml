spring:
  application:
    name: gateway

logging:
  file: ${LOG_FILE:gateway.log}
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
#    com.netflix: ${LOG_LEVEL_NETFLIX:WARN}
    com.netflix: DEBUG
    io.picos: ${LOG_LEVEL_APP:DEBUG}
