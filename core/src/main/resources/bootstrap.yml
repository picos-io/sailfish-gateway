spring:
  application:
    name: gateway

logging:
  file: ${LOG_FILE:gateway.log}
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.netflex: ${LOG_LEVEL_NETFLEX:WARN}
    io.picos: ${LOG_LEVEL_APP:DEBUG}

